# About
A React-based web application for remote controlling the PiPicoFX.

# Prerequisites
* nodejs v24 or higher
* npm


# build
using `npm run build`

# launch 
using `npm run dev -- --open --port 3000`